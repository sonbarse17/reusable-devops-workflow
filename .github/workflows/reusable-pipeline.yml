name: Reusable DevOps Pipeline

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target Environment'
        required: true
        type: choice
        options:
          - dev
          - uat
          - prod
        default: 'dev'
  workflow_call:
    inputs:
      environment:
        description: 'Target Environment'
        required: true
        type: string
      image_name:
        description: 'Docker image name'
        required: true
        type: string
      registry:
        description: 'Docker registry'
        required: false
        type: string
        default: 'ghcr.io'
      k8s_cluster:
        description: 'Kubernetes Cluster Name'
        required: true
        type: string
      k8s_namespace:
        description: 'Kubernetes Namespace'
        required: true
        type: string
      k8s_server:
        description: 'Kubernetes API Server URL'
        required: true
        type: string
    secrets:
      REGISTRY_USERNAME:
        required: true
      REGISTRY_PASSWORD:
        required: true
      KUBE_CONFIG_DATA:
        description: 'Base64 encoded kubeconfig'
        required: true

jobs:
  build:
    name: Build & Push
    runs-on: ubuntu-latest
    steps:
      - name: Validate Branch and Environment Mapping
        run: |
          BRANCH="${{ github.ref_name }}"
          ENV="${{ inputs.environment }}"
          if [[ "$ENV" == "prod" && "$BRANCH" != "release" ]]; then
            echo "Error: 'prod' only from 'release' branch." && exit 1
          elif [[ "$ENV" == "uat" && "$BRANCH" != "main" ]]; then
            echo "Error: 'uat' only from 'main' branch." && exit 1
          elif [[ "$ENV" == "dev" && "$BRANCH" != feature* && "$BRANCH" != "feature" ]]; then
            echo "Error: 'dev' only from 'feature' branch." && exit 1
          fi

      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Dependencies
        run: |
          if [ -f requirements.txt ]; then
            pip install --upgrade pip
            pip install -r requirements.txt
          fi

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ inputs.registry }}
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_PASSWORD }}

      - name: Build and Push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ inputs.registry }}/${{ inputs.image_name }}:${{ github.sha }}
            ${{ inputs.registry }}/${{ inputs.image_name }}:${{ inputs.environment }}

  deploy:
    name: K8s Deploy to ${{ inputs.environment }}
    runs-on: ubuntu-latest
    needs: build
    environment: ${{ inputs.environment }}
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Set Kubernetes Context
        uses: azure/k8s-set-context@v4
        with:
          method: kubeconfig
          kubeconfig: ${{ secrets.KUBE_CONFIG_DATA }}
          context: ${{ inputs.k8s_cluster }}

      - name: Deploy to Cluster
        run: |
          # Replace image in deployment and restart
          kubectl set image deployment/${{ inputs.image_name }} \
            ${{ inputs.image_name }}=${{ inputs.registry }}/${{ inputs.image_name }}:${{ github.sha }} \
            -n ${{ inputs.k8s_namespace }}
          
          kubectl rollout status deployment/${{ inputs.image_name }} -n ${{ inputs.k8s_namespace }}
